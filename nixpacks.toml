[phases.setup]
nixPkgs = ["php83", "php83Extensions.mysqlnd"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.build]
cmds = ["php artisan config:cache", "php artisan route:cache"]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
